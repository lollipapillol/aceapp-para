[build]
  command = "rm -rf deploy && mkdir -p deploy && unzip -q Aceapp-Parasitology-v42-prod1-aceapp-para.zip -d deploy && test -f deploy/index.html"
  publish = "deploy"

[build.environment]
  NODE_VERSION = "22"
